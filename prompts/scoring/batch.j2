You are a strict literary-quality judge. Score the PASSAGE below on each of
the listed DIMENSIONS. For each dimension, emit a score in [0.0, 1.0] and a
one-sentence rationale. Use the full 0..1 range — do not cluster around 0.5.

Return ONLY a JSON object of the form:
{
  "<dim_name>": {"score": <float 0..1>, "rationale": "<one sentence>"},
  ...
}
Include exactly the dimensions listed under DIMENSIONS — no more, no fewer.

Meta:
  work_id: {{ work_id }}
  is_quest: {{ is_quest }}
  pov: {{ pov }}

DIMENSIONS:
{% for d in dims %}
- {{ d.name }}
  {{ d.rubric | indent(2) }}
{% endfor %}

PASSAGE:
<<<
{{ passage }}
>>>

JSON:
