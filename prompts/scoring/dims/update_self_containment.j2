update_self_containment (quest-only): Does this update stand on its own —
opening with enough orientation and closing with a clear beat — without
requiring memory of prior updates? 0 = unreadable without context. 0.5 =
readable but lossy. 1.0 = crisply self-contained scene.
