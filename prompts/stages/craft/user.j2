## Dramatic Plan

**Action resolution:** {{ dramatic.action_resolution.kind }} — {{ dramatic.action_resolution.narrative }}
**Overall tension target:** {{ dramatic.update_tension_target }}
**Ending hook:** {{ dramatic.ending_hook }}

### Scenes

{% for scene in dramatic.scenes %}
#### Scene {{ scene.scene_id }}{% if scene.location %} — {{ scene.location }}{% endif %}

- **Dramatic question:** {{ scene.dramatic_question }}
- **Outcome:** {{ scene.outcome }}
- **Dramatic function:** {{ scene.dramatic_function }}
{% if scene.pov_character_id %}- **POV character:** {{ scene.pov_character_id }}{% endif %}

{% if scene.beats %}
**Beats:**
{% for beat in scene.beats %}
- {{ beat }}
{% endfor %}
{% endif %}
{% if scene.tools_used %}
**Tools used:** {{ scene.tools_used | join(", ") }}
{% endif %}
{% if scene.reveals %}
**Reveals:** {{ scene.reveals | join("; ") }}
{% endif %}
{% if scene.withholds %}
**Withholds:** {{ scene.withholds | join("; ") }}
{% endif %}
{% if scene.theme_ids %}
**Themes:** {{ scene.theme_ids | join(", ") }}
{% endif %}

{% endfor %}

---

## Emotional Plan

**Emotional arc:** {{ emotional.update_emotional_arc }}
**Contrast strategy:** {{ emotional.contrast_strategy }}

### Scenes

{% for scene in emotional.scenes %}
#### Scene {{ scene.scene_id }}

- **Primary emotion:** {{ scene.primary_emotion }} (intensity {{ scene.intensity }})
{% if scene.secondary_emotion %}- **Secondary emotion:** {{ scene.secondary_emotion }}{% endif %}

- **Entry state:** {{ scene.entry_state }}
- **Exit state:** {{ scene.exit_state }}
- **Transition type:** {{ scene.transition_type }}
- **Emotional source:** {{ scene.emotional_source }}
{% if scene.surface_vs_depth %}- **Surface vs depth:** {{ scene.surface_vs_depth }}{% endif %}

{% if scene.character_emotions %}
**Character emotions:**
{% for char_id, state in scene.character_emotions.items() %}
- {{ char_id }}: internal={{ state.internal }}, displayed={{ state.displayed }}{% if state.gap %} (gap: {{ state.gap }}){% endif %}

{% endfor %}
{% endif %}

{% endfor %}

---
{% if narrator %}

## Narrator (persistent — governs every scene)

This narrator is fixed for the entire quest. Derive each scene's
`narrator_focus`, `narrator_withholding`, and `sensory_palette` from
these fields rather than inventing them fresh.

- **POV type:** {{ narrator.pov_type }}
{% if narrator.worldview %}- **Worldview:** {{ narrator.worldview }}{% endif %}
{% if narrator.editorial_stance %}- **Editorial stance:** {{ narrator.editorial_stance }}{% endif %}
{% if narrator.register %}- **Register baseline:** {{ narrator.register }}{% if narrator.register_flex and narrator.register_flex[0] %} (flex: {{ narrator.register_flex[0] }} → {{ narrator.register_flex[1] }}){% endif %}{% endif %}
{% if narrator.knowledge_scope %}- **Knowledge scope:** {{ narrator.knowledge_scope }}{% endif %}
{% if narrator.withholding_tendency %}- **Withholding tendency:** {{ narrator.withholding_tendency }}{% endif %}
- **Reliability:** {{ narrator.reliability }}
{% if narrator.unreliability_axes %}- **Unreliability axes:** {{ narrator.unreliability_axes | join(", ") }}{% endif %}
{% if narrator.attention_bias %}- **Attention gravitates toward:** {{ narrator.attention_bias | join(", ") }}{% endif %}
{% if narrator.sensory_bias %}- **Sensory bias (target distribution):** {% for ch, w in narrator.sensory_bias.items() %}{{ ch }}={{ '%.2f'|format(w) }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{% if narrator.voice_samples %}

**Narrator voice samples:**
{% for sample in narrator.voice_samples %}
> {{ sample | trim }}

{% endfor %}
{% endif %}

---
{% endif %}
{% if style_register %}

## Style Register: {{ style_register.name }}

{{ style_register.description }}

- **Sentence variance:** {{ style_register.sentence_variance }}
- **Concrete/abstract ratio:** {{ style_register.concrete_abstract_ratio }}
- **Interiority depth:** {{ style_register.interiority_depth }}
- **POV discipline:** {{ style_register.pov_discipline }}
- **Diction register:** {{ style_register.diction_register }}

{% if style_register.voice_samples %}
**Voice samples** (model these rhythms and choices):
{% for sample in style_register.voice_samples %}
> {{ sample | trim }}

{% endfor %}
{% endif %}

---
{% endif %}
{% if character_voices %}

## Character Voices (grounded — do NOT invent bleed/excluded vocabulary)

{% for char_id, cv in character_voices.items() %}
### `{{ char_id }}`

- **Vocabulary level:** {{ cv.vocabulary_level }}
- **Sentence length bias:** {{ cv.sentence_length_bias }}
- **Directness:** {{ cv.directness }}{% if cv.uses_metaphor %} — uses metaphor{% endif %}
- **Emotional expression:** {{ cv.emotional_expression }}
- **Truth tendency:** {{ cv.truth_tendency }}
{% if cv.jargon_domains %}- **Jargon domains:** {{ cv.jargon_domains | join(", ") }}{% endif %}
{% if cv.signature_phrases %}- **Signature phrases:** {{ cv.signature_phrases | join("; ") }}{% endif %}
{% if cv.forbidden_words %}- **Forbidden words (never in this POV):** {{ cv.forbidden_words | join(", ") }}{% endif %}
{% if cv.code_switching %}- **Code-switches in:** {{ cv.code_switching | join("; ") }}{% endif %}

{% if cv.voice_samples %}**Character voice samples:**
{% for s in cv.voice_samples %}> {{ s | trim }}

{% endfor %}{% endif %}
{% if blended_samples.get(char_id) %}**Blended narrator+character few-shot (for POV passages):**
{% for s in blended_samples[char_id] %}> {{ s | trim }}

{% endfor %}{% endif %}
{% endfor %}

{% if scene_permeability_defaults %}
### Grounded voice_permeability defaults (use these bleed/excluded lists verbatim; adjust only baseline/current_target/triggers as scene demands)

{% for scene_id, vp in scene_permeability_defaults.items() %}
- **Scene {{ scene_id }}:** baseline={{ vp.baseline }}, bleed_vocabulary={{ vp.bleed_vocabulary | tojson }}, excluded_vocabulary={{ vp.excluded_vocabulary | tojson }}
{% endfor %}
{% endif %}

---
{% endif %}
{% if perceptual_profiles %}

## Character Perception (grounded — do NOT invent perceptual_preoccupations)

Each POV character's detail-selection bias is a stable property of who they are.
A soldier notices exits and sightlines; a merchant notices quality and wealth.
Derive the scene's `detail_principle.perceptual_preoccupations` from the
permanent list PLUS any emotion-activated entries for this scene's primary emotion.

{% for char_id, pp in perceptual_profiles.items() %}
### `{{ char_id }}`

- **Detail mode:** {{ pp.detail_mode }}
{% if pp.permanent_preoccupations %}- **Permanent preoccupations:** {{ pp.permanent_preoccupations | join(", ") }}{% endif %}
{% if pp.triple_duty_targets %}- **Triple-duty targets:** {{ pp.triple_duty_targets | join("; ") }}{% endif %}
{% if pp.emotional_preoccupations %}- **Emotional preoccupations:**
{% for emo, items in pp.emotional_preoccupations.items() %}  - *{{ emo }}* → {{ items | join(", ") }}
{% endfor %}{% endif %}
{% endfor %}

{% if scene_detail_defaults %}
### Grounded detail_principle defaults (use these preoccupation lists verbatim; refine detail_mode only if a stronger choice is warranted)

{% for scene_id, dp in scene_detail_defaults.items() %}
- **Scene {{ scene_id }}:** perceiver=`{{ dp.perceiving_character_id }}`, perceptual_preoccupations={{ dp.perceptual_preoccupations | tojson }}, triple_duty_targets={{ dp.triple_duty_targets | tojson }}
{% endfor %}
{% endif %}

---
{% endif %}
{% if metaphor_profiles_persistent %}

## Character Metaphor Profiles (grounded — do NOT invent permanent/forbidden domains)

Metaphor IS characterization. A character only draws from source domains they
have experience of. The `current_domains` for a scene are *computed* from the
character's permanent domains plus a small emotion-triggered set — not invented.

{% for char_id, mp in metaphor_profiles_persistent.items() %}
### `{{ char_id }}`

- **Permanent domains (life experience):** {{ mp.permanent_domains | join(", ") }}
{% if mp.forbidden_domains %}- **Forbidden domains (no experience — never use):** {{ mp.forbidden_domains | join(", ") }}{% endif %}
- **Metaphor density:** {{ "%.2f"|format(mp.metaphor_density) }} — {% if mp.extends_to_narration %}extends to narration during high permeability{% else %}stays in dialogue/interiority{% endif %}
{% endfor %}

{% if scene_metaphor_defaults %}
### Grounded metaphor_profiles defaults (use permanent/current/forbidden lists verbatim)

{% for scene_id, mp in scene_metaphor_defaults.items() %}
- **Scene {{ scene_id }}:** character=`{{ mp.character_id }}`, permanent_domains={{ mp.permanent_domains | tojson }}, current_domains={{ mp.current_domains | tojson }}, forbidden_domains={{ mp.forbidden_domains | tojson }}, metaphor_density={{ mp.metaphor_density }}
{% endfor %}
{% endif %}

---
{% endif %}
{% if tool_examples %}

## Tool Examples

{% for tool_id, examples in tool_examples.items() %}
### `{{ tool_id }}`

{% for ex in examples %}
> {{ ex.snippet | trim }}
> *({{ ex.annotation }})*

{% endfor %}
{% endfor %}

---
{% endif %}
{% if motives_by_scene %}

## Unconscious Motives (grounded — do NOT invent)

The following unconscious motives are part of persistent world state.
For each scene with a POV character listed below, produce one
`IndirectionInstruction` entry whose `unconscious_motive`,
`surface_manifestations`, `detail_tells`, and `what_not_to_say` are
drawn from the stored motive — not newly invented. `reader_should_infer`
should describe how the reader is meant to sense the motive without the
narration ever naming it.

{% for scene_id, motives in motives_by_scene.items() %}
### Scene {{ scene_id }}

{% for m in motives %}
- **motive id:** `{{ m.id }}`
  - **motive:** {{ m.motive }}
  - **surface manifestations:** {{ m.surface_manifestations | join("; ") }}
  - **detail tells:** {{ m.detail_tells | join("; ") }}
  - **what NOT to say:** {{ m.what_not_to_say | join("; ") }}
{% endfor %}
{% endfor %}

---
{% endif %}

{% if active_parallels %}

## Active Parallels (structural rhymes awaiting their B-half)

{% for p in active_parallels %}
- **{{ p.id }}** ({{ p.status.value if p.status.value is defined else p.status }}): A-half at update {{ p.source_update }} — "{{ p.source_description }}". Inversion axis: *{{ p.inversion_axis }}*. Target B-half: "{{ p.target_description }}"{% if p.target_update_range_min is not none and p.target_update_range_max is not none %} (due updates {{ p.target_update_range_min }}–{{ p.target_update_range_max }}){% endif %}.
{% endfor %}

When delivering a parallel's B-half, emit a ``parallel_instruction`` on the relevant scene with ``parallel_id`` set to the row id above so the delivery is persisted.

---
{% endif %}

{% if active_motifs %}

## Active Motifs (recurring images with evolving meaning)

{% for m in active_motifs %}
- **{{ m.motif.id }}** "{{ m.motif.name }}" — {{ m.motif.description }}. Semantic range: {{ m.motif.semantic_range | join(", ") }}. Target recurrence interval: {{ m.motif.target_interval_min }}–{{ m.motif.target_interval_max }} updates. {% if m.last_occurrence_update is not none %}Last seen at update {{ m.last_occurrence_update }} carrying meaning "{{ m.last_semantic_value }}".{% else %}Not yet used.{% endif %} {% if m.overdue %}**OVERDUE** — should recur now, ideally with a new semantic shade from its range.{% endif %}
{% endfor %}

When a motif appears in a scene, emit a ``motif_instruction`` on that scene with ``motif_id`` set to the id above and ``semantic_value`` chosen from the motif's semantic_range.

---
{% endif %}

{% if due_motifs %}

## Due/overdue motifs (consider weaving in)

The following motifs are due or overdue for recurrence at this update. Prefer weaving one or more of them into the prose — a returning motif usually lands harder when it arrives naturally inside an existing beat rather than as a standalone beat of its own.

{% for m in due_motifs %}
- **{{ m.name }}**: {{ m.description }} (last seen update {{ m.last_update_number if m.last_update_number is not none else "never" }}, semantic value: "{{ m.last_semantic_value if m.last_semantic_value is not none else "" }}", status: {{ m.status }})
{% endfor %}

---
{% endif %}

Produce the craft plan now. For each dramatic scene, emit one CraftScenePlan entry in `scenes` and one CraftBrief entry in `briefs`. The brief must be 100-300 words of prose that reads like a director's note — weave register, voice, emotional arc, and motif into a unified vision. Do not list parameters in the brief.
